import { Component } from "@angular/core";
import {RouterLink } from "@angular/router";

@Component({
    selector: "menu2",
    templateUrl: "./menu.html",
    styleUrl: './menu.css',
    imports: [RouterLink]

}) export class Menu {}